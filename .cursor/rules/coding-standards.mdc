---
description: 
globs: 
alwaysApply: false
---
# 编码规范指南

本文档定义了项目的编码规范和最佳实践。所有代码都应该遵循这些规范,以确保代码质量和一致性。

## 基本格式规范

### 缩进和空格
- 使用4个空格进行缩进
- 使用空格而不是制表符
- 文件末尾不强制要求空行
- 行尾使用CRLF换行符

### 大括号和换行
- 所有代码块都必须使用大括号
- 大括号独占一行
- catch、else、finally等关键字前需要换行
- 查询表达式子句之间需要换行

### 空格使用
- 二元运算符前后需要空格
- 逗号后需要空格,逗号前不需要
- 点号前后不需要空格
- 方法调用和方法声明中,括号与名称之间不需要空格
- 类型转换后不需要空格

## 命名规范

### 接口命名
- 接口名称必须以"I"开头
- 使用PascalCase命名风格
- 例如: `IUserService`, `IDataRepository`

### 类型命名
- 类、结构体、接口、枚举使用PascalCase
- 例如: `UserService`, `DataRepository`, `UserRole`

### 成员命名
- 属性、事件、方法使用PascalCase
- 例如: `GetUserById`, `UserName`, `OnUserCreated`

## 代码组织

### 文件组织
- 每个文件应该只包含一个主要的类型定义
- 相关的类型应该放在同一个命名空间中
- 使用partial类来组织大型类文件

### 命名空间组织
- 命名空间应该反映项目的文件夹结构
- 使用有意义的命名空间名称
- 避免过深的命名空间嵌套

### 类型成员组织
- 字段应该放在类的顶部
- 构造函数应该紧跟在字段之后
- 属性应该放在构造函数之后
- 方法应该按照功能分组
- 相关的方法应该放在一起

## 最佳实践

### 代码风格
- 优先使用表达式体成员(对于简单的属性和方法)
- 优先使用模式匹配而不是类型转换
- 优先使用switch表达式而不是switch语句
- 优先使用null检查而不是类型检查
- 优先使用范围运算符
- 优先使用元组交换
- 优先使用UTF8字符串字面量

### 性能考虑
- 使用readonly字段和结构体
- 优先使用静态匿名函数和本地函数
- 使用简单的using语句
- 优先使用System.Threading.Lock
- 优先使用主构造函数
- 优先使用顶级语句

### 错误处理
- 使用条件委托调用
- 使用throw表达式
- 正确处理未使用的值和表达式

### 现代化C#特性
- 使用模式匹配
- 使用元组
- 使用范围运算符
- 使用null合并运算符
- 使用字符串插值
- 使用集合表达式
- 使用主构造函数

## 代码质量

### 可访问性
- 非接口成员必须显式声明可访问性修饰符
- 合理使用访问修饰符(public, internal, private, protected)

### 代码简化
- 优先使用自动属性
- 优先使用对象初始化器
- 优先使用集合初始化器
- 优先使用条件表达式
- 优先使用复合赋值
- 优先使用简化的布尔表达式
- 优先使用简化的字符串插值

### 代码分析
- 启用所有建议的代码分析规则
- 定期运行代码分析工具
- 及时修复代码分析警告

## 注释规范

### 文档注释
- 所有公共API都应该有XML文档注释
- 文档注释应该清晰描述方法的功能、参数和返回值
- 使用标准的XML文档标签

### 代码注释
- 复杂的业务逻辑应该添加注释说明
- 非显而易见的代码应该添加注释
- 避免无意义的注释
- 保持注释的及时更新

## 测试规范

### 单元测试
- 所有公共API都应该有单元测试
- 测试方法名应该清晰表达测试目的
- 使用AAA模式组织测试代码(Arrange-Act-Assert)
- 每个测试应该只测试一个方面

### 测试命名
- 测试类名应该以"Tests"结尾
- 测试方法名应该遵循"Should_ExpectedBehavior_When_StateUnderTest"模式
- 例如: `Should_ReturnUser_When_ValidIdProvided`

## 版本控制

### 提交规范
- 提交信息应该清晰描述更改内容
- 使用有意义的提交信息
- 相关的更改应该放在同一个提交中
- 避免提交未完成的工作

### 分支策略
- 使用特性分支进行开发
- 主分支应该保持稳定
- 定期合并主分支的更改
- 使用拉取请求进行代码审查

